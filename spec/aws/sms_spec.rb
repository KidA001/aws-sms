require "spec_helper"

RSpec.describe Aws::Sms do
  it "has a version number" do
    expect(Aws::Sms::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
